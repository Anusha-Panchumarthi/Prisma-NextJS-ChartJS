export interface Instructor {
    id: string;
    name: string;
    dept_name: string;
    salary: string;
}

export interface Department{
    dept_name: string;
    building: string;
    budget: number;
}
